# SRE Agent Environment Variables
# Copy this file to .env and fill in your actual values

# User ID for session tracking
USER_ID=Alice

# Session ID for conversation grouping (auto-generated if not provided)
# SESSION_ID=session-12345

# Groq API Key for Groq Cloud models (primary provider)
GROQ_API_KEY=your_groq_api_key_here

# Anthropic API Key for Claude models (alternative provider)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# LLM Provider Configuration
# Options: groq (default), anthropic
LLM_PROVIDER=groq

# MCP Server URIs (Open-source OpenAPI-to-MCP servers)
# These are HTTP endpoints for each domain's MCP server
# In Kubernetes: use cluster-internal service DNS
# For local testing: use localhost with appropriate ports
MCP_K8S_URI=http://mcp-k8s.sre.svc.cluster.local:3000/mcp
MCP_LOGS_URI=http://mcp-logs.sre.svc.cluster.local:3000/mcp
MCP_METRICS_URI=http://mcp-metrics.sre.svc.cluster.local:3000/mcp
MCP_RUNBOOKS_URI=http://mcp-runbooks.sre.svc.cluster.local:3000/mcp

# Optional: Local development (comment out cluster URIs above and use these)
# MCP_K8S_URI=http://localhost:3001/mcp
# MCP_LOGS_URI=http://localhost:3002/mcp
# MCP_METRICS_URI=http://localhost:3003/mcp
# MCP_RUNBOOKS_URI=http://localhost:3004/mcp

# Optional: Debug settings
# DEBUG=true
# LOG_LEVEL=INFO


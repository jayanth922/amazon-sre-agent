apiVersion: v1
kind: ConfigMap
metadata:
  name: mcp-k8s-spec
  namespace: sre
data:
  k8s-openapi.yaml: |
    # Mount your K8s OpenAPI spec here
    # This will be replaced with the actual spec from backend/openapi_specs/
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-k8s
  namespace: sre
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mcp-k8s
  template:
    metadata:
      labels:
        app: mcp-k8s
    spec:
      containers:
        - name: server
          image: ghcr.io/jayanth922/mcp-openapi:local
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          env:
            - name: TRANSPORT_TYPE
              value: "http"
            - name: HTTP_PORT
              value: "3000"
            - name: ENDPOINT_PATH
              value: "/mcp"
            - name: API_BASE_URL
              value: "http://k8s-backend.sre.svc.cluster.local:8000"
            - name: OPENAPI_SPEC_PATH
              value: "/specs/k8s-openapi.yaml"
          volumeMounts:
            - name: specs
              mountPath: /specs
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
      volumes:
        - name: specs
          configMap:
            name: mcp-k8s-spec

apiVersion: v1
kind: ConfigMap
metadata:
  name: mcp-logs-spec
  namespace: sre
data:
  logs-openapi.yaml: |
    # Mount your Logs OpenAPI spec here
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-logs
  namespace: sre
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mcp-logs
  template:
    metadata:
      labels:
        app: mcp-logs
    spec:
      containers:
        - name: server
          image: ghcr.io/jayanth922/mcp-openapi:local
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          env:
            - name: TRANSPORT_TYPE
              value: "http"
            - name: HTTP_PORT
              value: "3000"
            - name: ENDPOINT_PATH
              value: "/mcp"
            - name: API_BASE_URL
              value: "http://logs-backend.sre.svc.cluster.local:8001"
            - name: OPENAPI_SPEC_PATH
              value: "/specs/logs-openapi.yaml"
          volumeMounts:
            - name: specs
              mountPath: /specs
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
      volumes:
        - name: specs
          configMap:
            name: mcp-logs-spec

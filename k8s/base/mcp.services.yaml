apiVersion: v1
kind: Service
metadata:
  name: mcp-k8s
  namespace: sre
spec:
  selector:
    app: mcp-k8s
  ports:
    - name: http
      port: 3000
      targetPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: mcp-logs
  namespace: sre
spec:
  selector:
    app: mcp-logs
  ports:
    - name: http
      port: 3000
      targetPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: mcp-metrics
  namespace: sre
spec:
  selector:
    app: mcp-metrics
  ports:
    - name: http
      port: 3000
      targetPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: mcp-runbooks
  namespace: sre
spec:
  selector:
    app: mcp-runbooks
  ports:
    - name: http
      port: 3000
      targetPort: 3000
